import React from "react";
import { View } from "react-native";

interface Props {
  id: string;
}

export const CryptoDetails: React.FC<Props> = ({ id }) => {
  return <View></View>;
};

// /coins/{id}/tickers
